Schema:(user shcema)
A schema is a blueprint for a collection.
It defines:
- what attributes (fields) a document can have
- what type of data each field can store
- validation rules and constraints for those fields
Examples of schema options in Mongoose:
    .type
    .required
    .trim
    .lowercase
    .maxlength
    .minlength
    .default
    .unique